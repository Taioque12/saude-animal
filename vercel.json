{
  "buildCommand": "cd cia-pet-app && npm install && npm run build",
  "outputDirectory": "cia-pet-app/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
