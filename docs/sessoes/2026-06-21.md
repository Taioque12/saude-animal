# Sessão 21/06/2026

## O que foi feito

### Dados reais da clínica
- Número de telefone real `(14) 3264-7135` substituído em Hero, FAQ e Footer
- CRMV mantido como genérico `CRMV-SP 00000` (pendente para próxima sessão)

### Seção de Agendamento
- Novo componente `landing/components/Agendamento.tsx`
- Cards de contato: Telefone, WhatsApp (com mensagem pré-preenchida) e Endereço
- Banner de horários: Seg–Sex 08h–18h · Sáb 08h–12h
- Âncora `#agendamento` movida do botão do Footer para a seção dedicada
- Adicionado em `page.tsx` entre `AreaRestrita` e `Footer`

### Correção de build
- `createClient` do Supabase estava no nível do módulo em `create-user/route.ts` e `delete-user/route.ts`
- Causava erro `supabaseUrl is required` em build time (Next.js avalia módulos na fase SSG)
- Solução: função `adminClient()` lazy — cliente criado dentro do handler

### Infraestrutura / Vercel
- Variáveis de ambiente do projeto `saude-animal` corrigidas no Vercel:
  - `NEXT_PUB_BASE_URL` → `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUB_ANON_KEY` → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- PR #1 mergeado para `main`
- Projeto duplicado `saude-animal` deletado do Vercel (estava com configuração quebrada)
- Projeto de produção ativo: `saude-animal-site` → https://saude-animal-site-lilac.vercel.app

## Pendências (Fase 1 — restante)
- [ ] CRMV real do Dr. Ighor Morales
- [ ] Endereço completo na seção de agendamento (rua e número)
- [ ] Testar criação de usuário no painel em produção
