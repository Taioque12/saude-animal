# Sessão 20/06/2026

## O que foi feito

### Área restrita — painel interno completo
- Login com Supabase Auth (split-screen, email/senha)
- Layout com sidebar fixa, drawer mobile e guard de autenticação
- 8 módulos completos: Dashboard, Agendamentos, Pacientes, Prontuários, Tutores, Vacinas, Estoque, Financeiro, Usuários
- Prontuários com receituário imprimível A4 via iframe
- Financeiro com export Excel (XLSX)
- Usuários: criação e exclusão de contas direto no painel (API routes server-side com service_role key)

### Visual
- Logo Saúde Animal sem fundo aplicada em: Navbar, Footer, Login, Sidebar do painel
- Animações suaves: modal-overlay, modal-panel, page-enter (globals.css)
- Sidebar mobile acelerada (200ms)
- Navbar altura ajustada para acomodar logo maior

### Infraestrutura
- Dependências: `@supabase/supabase-js`, `xlsx` adicionadas ao landing/
- Next.js atualizado para v16 (correção de vulnerabilidade de segurança)
- API routes: `/api/create-user` e `/api/delete-user` (Next.js App Router)
- Variável `SUPABASE_SERVICE_ROLE_KEY` adicionada ao `.env.local` e Vercel

### Deploy
- Novo repositório criado: https://github.com/Taioque12/saude-animal
- Deploy no Vercel: https://saude-animal-site-lilac.vercel.app
- CI/CD ativo — push no `main` faz deploy automático

## Decisões tomadas
- **Monorepo**: `landing/` (Next.js) + `cia-pet-app/` (Ionic) no mesmo repositório
- **Mesmo Supabase** para web e mobile — dados sempre sincronizados
- **Vercel** apenas para `landing/` — app mobile vai para App Store/Play Store via Capacitor

## Próxima sessão — Fase 1 (pendente)
- [ ] Substituir WhatsApp `5514999999999` pelo número real
- [ ] Substituir `CRMV-SP 00000` pelo CRMV real do Dr. Ighor Morales
- [ ] Criar seção de agendamento na landing page (âncora `#agendamento` já existe)
- [ ] Testar criação de usuário no painel em produção
